apiVersion: v1
kind: Secret
metadata:
  name: {{ template "cega.fullname" . }}-users-config
  labels:
    app: {{ template "cega.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
{{ ( .Files.Glob "conf/users.*" ).AsSecrets | indent 2 }}
---
{{- if .Values.tls }}
{{- $cafile := .Files.Glob (printf "conf/certs/%s" .Values.config.tls_ca_root_file)}}
{{- $cega := .Files.Glob "conf/certs/cega-users.*" }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "cega.fullname" . }}-users-certs
  labels:
    app: {{ template "cega.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
{{ ( $cafile ).AsSecrets | trim | indent 2 }}
{{ ( $cega ).AsSecrets | indent 2 }}
{{- end }}